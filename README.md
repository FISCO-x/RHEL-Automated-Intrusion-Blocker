# RHEL-Automated-Intrusion-Blocker
Developed a Python security daemon for RHEL environments to automatically detect and remediate SSH brute-force attacks. The script parses authentication logs using regex to isolate malicious IPs, interfacing directly with firewalld to permanently block threats with zero manual intervention.
